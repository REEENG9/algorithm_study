#include <stdio.h>

int main (){
    int a;
    scanf("%d", &a);
    printf("%d", !a);

    return 0;
}

// 논리연산자 : 참, 거짓의 논리값(boolean value)인 불 값을 다룸
// !(not), &&(and), ||(or)

// 참 또는 거짓의 논리값을 역(반대)으로 바꾸기 위해서는
// 논리(logical)연산자 !를 사용할 수 있다.